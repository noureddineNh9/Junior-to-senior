/*

> redis is an in memory storage. So that means it's super super fast for us to access.

> We can get values really really fast from it but it has that downside where it only takes a snapshot and writes it on disk occasionally so you might lose some data if the server goes down.

> redis is often used for small pieces the data like session management or maybe even caching some small data.


* redis

- we can expise variables ''

*/
